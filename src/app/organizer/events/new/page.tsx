import { SectionPage } from "@/components/layout/section-page";

export default function NewOrganizerEventPage() {
  return (
    <SectionPage eyebrow="Organizer" title="Create event">
      Start with the race name, date, location, and available distances.
    </SectionPage>
  );
}
