# ZidRun Release Plan

> **The only source of truth for progress, priorities, release gates, and future TODOs.**
> Do not create another backlog, phase plan, audit TODO, or progress file. Supporting documents may
> explain how to test or operate the product, but every open action belongs here.

**Last updated:** 2026-08-01

**Release readiness:** `█████████████░░░░░░░░░░░░` **52% — 31 of 60 gates complete**

**Security hardening overlay:** **0 of 15 P0 security gates complete** — release remains on HOLD until
the applicable gates below have evidence.

**Status:** **HOLD** until the P0 release gates below are complete and recorded against one immutable
commit. The web platform is live; this plan governs the next web deployment and Android production
rollout.

## What to do next

Work from top to bottom. Do not start P1/P2 product work while an unblocked P0 item can be closed.

| Order | Priority | Action | Owner | Done when |
|---:|---|---|---|---|
| 1 | P0 | **Freeze the release scope and get a green remote candidate** (`PR-056`, `PR-057`) | Engineering | Groups commit `4f0453a` is included by owner decision. Finish public/private group, join-link, invitation, authorization, privacy, moderation, and mobile acceptance; push every intended commit; pass remote CI; tag the exact candidate. |
| 2 | P0 | **Confirm ZidRun production access in Play Console** (`PR-052`) | Owner | After Elmohassib's production-access decision, create/open ZidRun and inspect its Dashboard/Production page. If Production is unlocked, upload ZidRun without repeating the 12-tester cycle; if ZidRun shows its own production-access gate, start its closed track and follow the exact tester requirement shown by Play Console. In either case, review ZidRun's pre-launch, crash/ANR, and policy reports. |
| 3 | P0 | **Run signed physical-device acceptance on 2026-07-30** (`PR-050`, `RUN-001`–`RUN-006`) | Engineering + owner | The exact signed candidate passes auth, deep links/back navigation, safe areas, Groups, voice test, guided warm-up/work/cool-down, background GPS, pause/resume, force-kill restore, logout/account switching, sustained non-foot auto-pause, stationary-start drift, Google Drive/local GPX import, GPS cold-start/map settling, compact run charts, pending-finish recovery, mobile input focus, immediate saved-run visibility, and Runs redesign parity across themes, locales, and RTL. Record device/OS/build and results in this file. |
| 4 | P0 | **Verify native production integrations after device acceptance** (`PR-048`, `PR-049`) | Owner + engineering | With Codex assistance: hosted `assetlinks.json` contains the Play App Signing SHA-256; production push reaches the signed app; notification taps route correctly; a Crashlytics test event appears. |
| 5 | P0 | **Prove data recovery and private storage** (`PR-020`–`PR-025`) | Operations | Rehearse `prisma migrate deploy`; verify automated database backup and restore; make uploads durable; protect payment/private media; document retention, export, deletion, and production access; remove temporary bootstrap credentials. |
| 6 | P0 | **Complete the security hardening overlay** (`SEC-001`–`SEC-015`, plus `PR-031`, `PR-041`) | Engineering + owner + external reviewer | Close the privacy, identity, application, API, database, infrastructure, monitoring, recovery, attack-testing, and independent verification gates below. Existing controls count only after they are tested and recorded. |
| 7 | P0 | **Run controlled web acceptance** (`PR-034`, `PR-036`–`PR-043`) | Product + engineering | On the exact candidate, pass runner registration, capacity/payment/cancellation, organizer lifecycle, admin moderation, Coach/voice/safety, EN/FR/AR/RTL/themes/accessibility, deployment, production smoke, monitoring, backup, and rollback checks. |
| 8 | P0 | **Finish store and rollout governance** (`PR-051`, `PR-053`, `PR-058`–`PR-060`) | Owner | Listing, screenshots, privacy/Data Safety, deletion URL, version/release notes, staged percentage, rollback procedure, incident owner, escalation contacts, and final go/no-go are approved. |

## Progress by gate group

These counts preserve the existing 60-gate release baseline. A gate moves only when evidence exists;
finishing code alone is not enough for a device, operations, or rollout gate.

| Gate group | Complete | Total | Open gate IDs |
|---|---:|---:|---|
| Code quality and release branch | 14 | 14 | — |
| Platform, data, and secrets | 6 | 11 | `PR-020`–`PR-023`, `PR-025` |
| External services and observability | 4 | 8 | `PR-030`–`PR-033` |
| Controlled acceptance and production | 1 | 10 | `PR-034`, `PR-036`–`PR-043` |
| Android and Play Store | 4 | 10 | `PR-048`–`PR-053` |
| Release governance | 2 | 7 | `PR-056`–`PR-060` |
| **Total** | **31** | **60** | **29 open** |

### Completed baseline

- `PR-001`–`PR-014`: dependency audit, lint/i18n, typecheck, domain tests, Playwright, visual
  regression, production build/smoke, deterministic CI data, disabled placeholder mutations,
  supported Sentry instrumentation, and self-hosted fonts are complete.
- `PR-015`–`PR-019`, `PR-024`: production web hosting, HTTPS, PostgreSQL, secrets/canonical auth,
  and scheduled cron jobs are operational.
- `PR-026`–`PR-029`: verification email, OpenAI billing/limits, Sentry alerts, and uptime monitoring
  are operational.
- `PR-035`: owner confirmed register → email → verify → login in production.
- `PR-044`–`PR-047`: Capacitor production identity, native shell/plugins, release bundle, and clean
  production sync are verified.
- `PR-054`, `PR-055`: release procedure and this maintained dashboard exist.

## P0 — security hardening overlay

This is a defense-in-depth plan, not a promise of perfect security. The goal is to minimize owner
exposure, minimize the data ZidRun holds, make unauthorized access difficult, limit blast radius, and
detect and recover from attacks. Use the [OWASP ASVS 5.0](https://owasp.org/www-project-application-security-verification-standard/)
as the application verification baseline, the [OWASP API Security Top 10](https://owasp.org/API-Security/)
for route review, and the [OWASP Authentication](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)
and [Session Management](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)
guidance for identity controls.

Do not mark a gate complete from code inspection alone. Each closed gate needs a commit or configuration
change, a test/scan result, and production evidence where the control depends on Cloudflare, Caddy,
PostgreSQL, Docker, backups, or the owner’s accounts.

| Gate | Priority | Hardening outcome | Required evidence / acceptance |
|---|---|---|---|
| `SEC-001` | P0 | **Owner privacy and public metadata lockdown.** Remove owner personal identity, email, phone, home/location clues, local paths, infrastructure names, debug routes, framework/version banners, source maps, verbose error details, and accidental author metadata from public HTML, headers, manifests, emails, APK metadata, Git hosting, analytics, and Sentry. Use role accounts/aliases and a dedicated support/security contact; keep the owner’s personal account out of public-facing records. | Public-site metadata/header/error crawl plus `robots.txt`/sitemap review; inspect public repo/history and deployment artifacts; confirm `x-powered-by`/debug/source-map leakage is absent; owner privacy checklist signed. Do not rely on hiding the admin path as the control. |
| `SEC-002` | P0 | **Data inventory, minimization, and lifecycle.** Classify credentials, identity fields, contact data, payment proofs, health/coach data, precise GPS/GPX, social data, logs, analytics, notifications, and backups. Define purpose, access role, retention, deletion, export, legal hold, and subprocessor/provider handling for every class. Default runner profiles and activity to private unless the user explicitly publishes them. | A maintained field-level inventory in this plan or an approved product/operations reference; privacy-policy alignment; working export/delete/revoke flows; retention jobs tested; no health-memory writes until consent and sports-health gates close. |
| `SEC-003` | P0 | **Authentication and session lockdown.** Require phishing-resistant MFA (passkey/security key) for every owner/admin/superadmin account; TOTP is only an explicitly time-limited fallback. Protect enrollment/recovery, rotate/revoke sessions after password/MFA/role changes, enforce idle and absolute session limits, use Secure/HttpOnly/SameSite cookies, prevent account/email enumeration, harden reset/verification/native handoff tokens, and add breached-password/credential-stuffing defenses. | `npm run test:mfa` plus focused tests for reset, verification, OAuth linking, native handoff, logout, session revocation, callback URLs, enumeration, and brute force; production admin MFA proof; no bootstrap credentials remain. |
| `SEC-004` | P0 | **Authorization and tenant isolation.** Review every page, action, API route, object ID, organization membership, admin function, coach record, support thread, payment proof, run/GPX, group, notification, and export for BOLA/BFLA/IDOR. Enforce deny-by-default server-side checks and narrow selects; never trust role or ownership from client input. | Route/object authorization matrix; negative tests using runner A, runner B, organizer A/B, admin, and superadmin; export/private-media denial tests; audit entries for sensitive admin/member changes. |
| `SEC-005` | P0 | **CSRF, origin, CORS, and browser boundary lockdown.** Add explicit origin/fetch-metadata checks for state-changing browser requests, verify Auth.js CSRF behavior for all credential flows, allow only required origins/methods/headers, and ensure no state-changing GET. Keep CSP nonce-based where practical; retain HSTS, frame denial, nosniff, referrer, permissions, and upload sandbox policies. | Header scanner and browser tests for XSS/clickjacking/CSRF/CORS; production checks through Cloudflare → Caddy → Next; CSP report-only rollout followed by enforced policy with zero unexplained violations. |
| `SEC-006` | P0 | **API abuse and resource controls.** Apply shared edge + application rate limits to login, reset, verification, registration, uploads, coach/AI/audio, search/import, support, reports, social actions, notifications, and exports. Add request-size, field-length, pagination, timeout, concurrency, and per-user/storage quotas. Use one app instance until limits are shared. | Route inventory with limits; Cloudflare rules and 429 evidence; load/abuse tests for credential stuffing, scraping, spam, upload exhaustion, AI cost abuse, race oversell, and slow requests; Redis/edge-backed limiter or documented single-instance boundary. |
| `SEC-007` | P0 | **Input, output, and file safety.** Keep Zod validation and parameterized SQL everywhere; review raw SQL; prevent XSS/HTML/MDX injection, template injection, prompt injection, SSRF through imported URLs, unsafe redirects, CSV formula injection, and log injection. Keep uploads outside executable paths, re-encode images, strip metadata, enforce quotas, and make all private media authenticated/signed and non-cacheable. | ASVS/API-focused code review; SAST/dependency/secret scans; malicious corpus tests for images, URLs, CSV, Markdown, GPX, prompts, and oversized bodies; verify no public payment-proof or precise-location URL. |
| `SEC-008` | P0 | **Database isolation and least privilege.** Put PostgreSQL on a private network with no public 5432; use a dedicated runtime role with only required privileges, separate migration/backup roles, TLS in transit, encrypted volumes, connection limits, safe pool sizing, statement/timeouts, and guarded admin access. Remove sample/demo credentials and rotate all production secrets. | Firewall/security-group output; role grants audit; encrypted-volume/DB TLS proof; migration/backup roles tested; secret inventory and rotation record; no secrets in Git, images, logs, client bundles, or `NEXT_PUBLIC_*`. |
| `SEC-009` | P0 | **Backup, deletion, and recovery protection.** Encrypt database and upload backups, keep immutable/offsite copies with limited access, define RPO/RTO, alert on backup failure, test restore into isolation, and verify deletion/retention behavior across primary DB, uploads, backups, email, Sentry, analytics, AI provider, and push systems. | Restore drill with backup ID, timestamp, operator, duration, row/media checks, and result; documented RPO/RTO; deletion and legal-retention decision; no backup accessible from the public web. |
| `SEC-010` | P0 | **Upload and private-data delivery lockdown.** Keep payment proofs, health-adjacent media, GPX, and any non-public user file behind authorization rather than Caddy’s public static handler; use short-lived signed/authenticated delivery, `Cache-Control: private/no-store` where appropriate, access logging without sensitive content, safe `Content-Disposition`, and separate storage prefixes/buckets. | Direct URL, referrer, cache, range-request, MIME, traversal, and cross-account tests; Cloudflare/Caddy config review; confirm the current public `/uploads/*` exception cannot expose a newly added private scope. |
| `SEC-011` | P0 | **Edge, host, and container lockdown.** Cloudflare should be the only public origin path; restrict origin firewall ports, SSH to keys/approved sources, disable root/password SSH, patch the host and images, run containers as non-root with minimal capabilities/read-only mounts where possible, separate upload volume permissions, and remove unused services/routes. | External port scan; origin-IP bypass test; host/container hardening checklist; patch status; Docker/Caddy config review; rollback tested without restoring insecure defaults. |
| `SEC-012` | P0 | **Supply-chain and release integrity.** Pin and audit dependencies, enable lockfile/dependency/security/secret scanning, protect the main branch and deployment secrets, review Prisma migrations/raw SQL, generate an SBOM, prevent source-map/debug artifact publication, and deploy only an owner-approved immutable commit. | Green `npm audit`, lint, typecheck, tests, build; CI scan logs; SBOM; migration review; branch/CI settings; exact production commit and rollback artifact recorded. |
| `SEC-013` | P0 | **Detection, logging, and incident response.** Centralize structured security events while redacting passwords, tokens, payment images, health text, exact GPS, email/phone, and unnecessary IP/device data. Alert on login failures, reset abuse, MFA changes, privilege changes, exports, private-file denials, rate-limit spikes, DB failures, cost anomalies, and suspicious admin activity. Prepare breach containment, account lock, secret rotation, restore, user notification, and law/regulator escalation runbooks. | Synthetic alerts delivered; log-retention/access review; Sentry scrubbing verified; incident tabletop for account takeover, DB leak, ransomware/backup loss, DDoS, upload abuse, and insider misuse; named incident owner and escalation contacts. |
| `SEC-014` | P0 | **Independent verification and recurring maintenance.** Run an external penetration test or qualified security review against web/API/mobile/host boundaries, remediate critical/high findings, and repeat dependency, secret, exposure, backup, access, and authorization reviews on a fixed cadence. | Signed report, severity-based remediation evidence, retest result, quarterly access review, monthly dependency/secret scan, recurring external attack-surface scan, and annual restore/incident exercise. |
| `SEC-015` | P0 | **Execute the controlled attack-test plan before production release and marketing.** Run the repository’s [Security Attack-Test Plan](docs/SECURITY_ATTACK_TEST_PLAN.md) against the exact candidate, using isolated staging for active scans/abuse/load tests and production only for approved passive checks. | The plan’s source/dependency, secret, container, exposure, ZAP, auth/authz, privacy, upload, API-abuse, k6, database, backup, host, Android, and marketing checks are complete; no Critical/High findings remain; Medium findings have approved expiry; sanitized evidence is linked here. |

### Attack coverage required before security sign-off

The acceptance set must explicitly exercise: credential stuffing, phishing-resistant admin takeover,
session theft/fixation/replay, MFA recovery abuse, email/account enumeration, CSRF, XSS, injection and
unsafe SQL, SSRF, malicious redirects, BOLA/IDOR/BFLA, privilege escalation, organization data leakage,
payment/health/GPS exposure, malicious uploads and metadata leakage, scraping/spam, AI prompt and cost
abuse, race-capacity oversell, notification abuse, DDoS/resource exhaustion, dependency/supply-chain
compromise, container/host escape, database compromise, backup theft/ransomware, insider misuse, and
third-party/provider compromise. “No known vulnerability” is not evidence; record the test, result,
remaining risk, owner, and expiry date for every exception.

### Security decisions that are locked for this release

- Phishing-resistant MFA is mandatory for owner/admin/superadmin accounts; TOTP is only a temporary
  fallback with an expiry date and documented exception.
- The database and origin are private; public exposure of PostgreSQL or direct origin access fails release.
- User private data is not served as public static content. Public race imagery is the only default-public
  upload class; every other class needs an explicit privacy decision and access test.
- Precise GPS/GPX, payment proofs, health/coach context, tokens, and credentials never appear in public
  pages, client bundles, analytics, logs, error reports, emails, or backups without access controls.
- Security-through-obscurity is not accepted as a control: hiding owner/admin routes or framework names
  reduces reconnaissance but does not replace authentication, authorization, MFA, rate limiting, or WAF.
- No new data-collecting feature, third-party SDK, public profile surface, or AI memory field ships until
  its data classification, consent, retention, deletion/export, access, and provider-processing decisions
  are recorded here and tested.

## P0 acceptance details

### Exact release quality gate

Run against the release candidate:

```bash
npm audit --audit-level=low
npm run lint
npm run typecheck
npm run test:all
npm run build
npm run smoke
```

Then follow [docs/TESTING.md](docs/TESTING.md), [docs/OPERATIONS.md](docs/OPERATIONS.md), and
[docs/MOBILE_ANDROID.md](docs/MOBILE_ANDROID.md), plus the [security attack-test plan](docs/SECURITY_ATTACK_TEST_PLAN.md).
Browser automation does not close a physical-device
gate, and a debug APK does not close a signed-release gate.

### P0 mobile field findings

These findings came from the 2026-07-30 physical-device run and are part of `PR-050`; they do not
change the 60-gate release baseline. The supplied GPX is retained as local test evidence only and
must not be committed because it contains precise location data.

| ID | Finding / required behavior | Implementation and acceptance |
|---|---|---|
| `RUN-001` | Indoor stairs and movement inside a building may be below reliable GPS resolution, but a cached first fix must not show the runner in the wrong city. The 2026-07-30 local GPX converges to the current city after the outdoor warm-up; this is a GPS cold-start/settling issue, not a requirement to infer indoor movement. | Keep replacing an unconfirmed startup fix, suppress the live map until a stable route exists, show GPS-acquiring feedback, and retest on a signed device in two cities/indoors-to-outdoors. Pass when no stale city is shown as the route and valid outdoor movement is retained. |
| `RUN-002` | Run elevation and pace charts need compact, readable X/Y reference information without making the small mobile card dense. | Add minimal endpoint/tick labels and axes to both profiles, preserve dark/light/race/RTL readability, and verify the expanded run detail against the supplied screenshots. |
| `RUN-003` | After Finish, the run must not silently disappear or upload without the runner choosing. | Keep the finished snapshot recoverable locally, show “not saved yet” copy with explicit Save/Discard actions, and verify app background/return/force-close behavior before either action. |
| `RUN-004` | On mobile, focused inputs must remain visible above the keyboard and keep the active field in view across the app. | Add native-only focus/visual-viewport scrolling plus focused-field spacing, then verify text, textarea, select, range, and checkbox flows in account, organizer, admin, registration, Coach, and run screens. |
| `RUN-005` | A successfully saved GPS run did not appear in the visible history until the runner manually refreshed. | Insert the returned run into local history immediately, bring it into view, expand/highlight it, and keep the background refresh reconciliation. Pass when a saved run is visible in the history without reload and remains after refresh. |
| `RUN-006` | The Runs experience needs a clearer dashboard, a motivating start interaction, glanceable live metrics, and a more useful history/detail journey without losing the existing run-save and recovery behavior. | Implement the Runs overview, press-and-hold start interaction, live distance/time hero, search/filter history, theme-safe light/dark/race styling, and EN/FR/Algerian Arabic copy with RTL support. Pass when the signed candidate verifies overview → start → live → finish/save → history/detail on a small phone, with keyboard focus and accessibility checks intact. |

### Release decisions that are already locked

- Payments remain manual for this release; do not add a gateway now.
- Registration resale, bib transfer, and a goods marketplace are out of release scope.
- iOS is not part of this release; it needs a Mac/Xcode and a separate distribution track.
- A separate staging deployment is not required. Acceptance must still run against the exact commit
  that is promoted through the owner-approved production-safe process.
- Capacitor remains the current Android release path. The native Android option is isolated and deferred;
  it does not block the current Capacitor release until a later decision gate approves a switch.
- Coach health-memory writes remain disabled until explicit consent, retention, export/deletion, and
  sports-health review are complete.
- Raw GPX routes and other precise location evidence must not be committed.
- Groups commit `4f0453a` is in the Android release scope and must pass the same signed-candidate
  authorization, privacy, moderation, and mobile acceptance as the existing release features.

### Parallel launch preparation (owner/marketing)

These do not change the 60 engineering gates, but they must be ready before the public announcement:

1. Confirm the Coach Zid persona/name/catchphrase with native runners and choose real, synthetic, or
   hybrid presentation with transparent AI disclosure.
2. Claim/complete Instagram, Facebook, and TikTok profiles; prepare the link-in-bio destinations.
3. Onboard enough organizers and real races that launch discovery is not empty.
4. Prepare reviewed store/social screenshots, demo recordings, Week 1–2 content, reusable templates,
   and EN/FR/AR copy.
5. Configure privacy-conscious UTM/activation reporting and assign the launch response/feedback owner.

## P1 — first work after release

Do these only after P0 is closed or when a P0 item is externally blocked.

1. **Bound growing data surfaces:** paginate public races, organizer collections, long run histories,
   and any remaining conversation/feed endpoints; add indexes from measured query patterns.
2. **Complete accessibility and RTL acceptance:** 44 px touch targets, focus visibility, contrast,
   logical spacing, directional-icon mirroring, loading/error/empty states, and EN/FR/AR copy parity.
3. **Coach personalization after governance:** health-memory expiry/reconfirmation, progressive
   profile prompts, opt-in coarse location/timezone, and qualified safety review.
4. **Notifications:** payment-proof review notices, race reminders, delivery/idempotency hardening,
   and final signed-device push routing.
5. **Groups after launch:** add richer discovery and engagement only after the release-scope private/public,
   invitation, authorization, privacy, moderation, notifications, migrations, browser tests, and mobile UX are accepted.
6. **Growth:** shareable run/plan cards, post-trial free allowance, and trial-ending lifecycle nudges.

## P2 — later roadmap

- Public runner profiles, clubs/groups discovery, registered-runner blog comments, and remaining
  EN/FR/AR editorial content.
- Social-post Android/PWA Share Target after the existing admin import flow is stable.
- Strava/wearable/FIT sync after provider registration, consent, deduplication, and import provenance.
- Coach location personalization, travel-to-race advice, adaptive check-ins, and Coach as the primary
  runner home.
- Object storage plus PgBouncer/multiple app replicas and k6 registration-open load testing before a
  high-concurrency race. Do not scale app replicas while rate limits and uploads remain process-local.
- Marketplace and bib transfer only after fraud, organizer approval, refunds, and payment rules have
  a separate approved specification.

## P2 — native Android option evaluation

Capacitor remains the release fallback while this option is evaluated. The detailed implementation and
backend contract are in the [Native Android Option Plan](docs/NATIVE_ANDROID_OPTION_PLAN.md). These
workstreams do not change the current 60-gate release baseline and must not introduce breaking API or
database changes for the website or Capacitor app.

| ID | Priority | Workstream | Done when |
|---|---|---|---|
| `NATIVE-001` | P2 | Freeze the Capacitor reference | Exact Capacitor commit, signed artifact, feature/route matrix, native bridge inventory, device results, and known limitations are recorded. |
| `NATIVE-002` | P2 | Create an isolated native Android project | `native-android/` or a separate repository has its own package, Gradle flavors, Firebase app IDs, signing boundary, privacy-safe logging, and no production secret or keystore copied from Capacitor. |
| `NATIVE-003` | P2 | Define additive `/api/v1` contracts | OpenAPI/DTO/error/pagination/rate-limit contracts exist for auth, profile, races, registrations, uploads, runs, coach, notifications, and approved social features; website/server actions remain compatible. |
| `NATIVE-004` | P2 | Implement native auth and device sessions | PKCE/system-browser OAuth, short-lived access tokens, rotating/revocable refresh tokens, MFA, logout-all, account switching, device registration, and session revocation pass security tests. |
| `NATIVE-005` | P2 | Implement native run/offline/media foundations | Foreground GPS service, Room outbox, WorkManager sync, idempotency/conflict rules, GPX/media upload boundaries, recovery, and private storage pass physical-device and privacy tests. |
| `NATIVE-006` | P2 | Reach measured feature and operational parity | Native vertical slice and then agreed feature scope pass Capacitor comparison, API contract, accessibility/RTL, crash/ANR, battery, performance, security, backup, and rollback checks. |
| `NATIVE-007` | P2 | Prove parallel release safety | Separate internal/closed-track package, App Links/Firebase fingerprints, Data Safety/privacy copy, staged rollout, support/monitoring, and Capacitor rollback path are verified. |
| `NATIVE-008` | P2 | Make the switch/continue decision | Weighted comparison is completed with evidence; owner chooses Capacitor, native primary, or both; the decision, migration/retirement plan, and remaining risk are recorded in this file. |

### Owner decision — native Runs adopts the website's visual system (2026-08-01)

The owner reviewed the trade-off and chose a **full restyle of the native Runs overview to match the
Capacitor/web page**, in preference to an icons-only alignment. This is recorded because it changes an
acceptance reference: **`docs/runs-design/images/01-runs-overview.png` (the ring-hero design) no longer
governs the native Runs overview** — the website's `RunsOverview` does. The mockup still governs the
web page it was drawn for, and the other native Runs screens (create/record/summary/detail/history)
are unchanged and still governed by their own approved images.

What was already aligned and needed no work: both clients render **Manrope** from the same variable
font, and the native type scale already mirrors the web's, including the 600-weight cap.

What changed: the overview now uses the website's header block (teal eyebrow, week title, momentum
line, history link), its dark week hero with the flame accent and fill bar, the runs-count card with
the streak line, the latest-run card with three divided metrics, and the "Personal bests" card. Native
keeps its **ZidRun wordmark** brand bar rather than adopting the website's typed `<h1>` header, because
that header is a logged defect (`RUNPAR-008`) and the review forbids copying a known Capacitor defect.
GPX import keeps a row of its own rather than being dropped, since the website still offers it further
down its page. Icons are now **Lucide**, the same family the website uses — six vector drawables
(`ic_flame`, `ic_footprints`, `ic_timer`, `ic_award`, `ic_route`, `ic_arrow_up_right`) with geometry
copied verbatim from `lucide-react` v0.468 rather than redrawn.

**A real defect was found by this work and fixed:** native computed "this week" with
`WeekFields.of(Locale.getDefault())`, which starts the Arabic week on **Saturday**. The same runner on
the same day therefore saw "1 run · 1.2 km" in English and "0 runs · 0.0 km" in Arabic, and both
disagreed with the website, which is Monday-based for every locale. Weekly totals and the streak are
now `WeekFields.ISO`, so the number no longer depends on the language the app is read in. Caught only
because the restyle was verified in Arabic on the device.

Two narrow-screen truncations were also fixed on the way (the latest-run metric cells hard-clipped
"DISTANCE" to "DISTA" and "3:59/km" to "3:59/"); the remaining "TOTAL DISTA…" on a 320dp screen now
ellipsizes visibly instead of cutting silently.

**Bottom navigation** was brought to the same treatment, from `.mobile-tab*` in `globals.css` rather
than from the screenshot: a 52×28dp icon pill tinted at 16% primary when active, and the Runs tab as
the website's primary tab — 58×34dp, raised 4dp, tinted at 12% even when unselected and solid primary
with a `surface-strong` glyph when active. Tab glyphs are the website's own Lucide set
(`calendar-search`, `footprints`, `sparkles`, `user-round`), replacing the Material icons.

**Run detail charts** now follow `docs/runs-design/images/05-run-details.png`: five y-ticks with
horizontal gridlines behind a filled profile, kilometre x-ticks (dropped when they would collide on a
narrow screen), a dashed average reference on the pace chart, a Gain/Loss line above the elevation
profile, and a Δ column on the splits table showing each split against the run's own average —
red for slower, green for faster, with the sign carrying the meaning independently of colour. The
pace chart's dashed average is suppressed when the run's stored average falls outside its own
plotted series (it did on the test run: 3:59 average against a 4:08–4:16 series), because clamping
the reference to an edge would put it somewhere the runner never ran. Elevation gain and loss are
both summed from the plotted series rather than pairing the server's noise-filtered `elevationGainM`
with a raw client loss — that pairing printed "Gain 0 m  Loss 27 m" for one flat run, two numbers
measured different ways sitting side by side.

**Gates:** `assembleDebug` clean, `lintDebug` 0 errors across all 8 modules, 31 native unit tests,
`check:native-i18n` 379 keys at en/fr/ar parity. **Verified on device** in light, dark, and race, and
in English, French, and Arabic RTL (mirroring confirmed, including the hero fill bar filling from the
right and the route thumbnail correctly not mirroring).

### Open findings from the 2026-07-31 parity review (`NATIVE-006`)

Found on a Pixel_8 emulator (Android 17 / API 37, 320×640 @160dpi) at `ef0cece`, native
`dz.racedz.nativeapp.debug` v0.7.0. The four defects fixed in the same session are described in the
evidence row below and are not repeated here. All of these are native-only unless stated.

| ID | Severity | Finding, reproduction, and expected behaviour | Proposed fix and acceptance |
|---|---|---|---|
| `RUNPAR-001` | P1 | **The press-and-hold start control clips its own label.** Runs → Record run: the green foot outline passes straight through "Hold", and "to begin" overflows both edges. The approved reference (`docs/runs-design/images/02-create-new-run-hold-ready.png`) gives the label clear space inside a wide shoe-sole silhouette and ends with a double chevron; the implementation draws a narrow egg with the text on top of the stroke. This is the primary action of the Runs flow, so the legibility cost lands on the most important control. Hold progress, orange glow, and the green ring themselves are correct. | Widen/reshape the silhouette to the approved sole outline and lay the label out inside its inner bounds rather than the control's outer bounds. Accept when ready / mid-hold / complete all render the label fully clear of the stroke at 320dp in en, fr, and ar, and with a 1.3× font scale. |
| `RUNPAR-002` | P2 | **Run history rows drop the distance unit when the run is an hour or longer.** Run history at 320dp: a row with duration `56:06` renders "11.00 km", while `1:01:24` renders "12.00" with " km" not painted. The accessibility node still reports "12.00 km", so only sighted users lose it. Cause: three `RowStat`s share the row on equal `weight(1f)` thirds, and the wider duration string pushes the distance text past its cell. The code comment at `RunHistoryScreen.kt:200` shows this row has already clipped once before. | Give the three stats intrinsic widths distributed with `Arrangement.SpaceBetween` instead of equal thirds — measured content ("12.00 km" 48px + "5:07/km" 46px + "1:01:24" 40px) fits the 188px row comfortably. Accept when a ≥1 h run shows its unit at 320dp in all three locales. |
| `RUNPAR-003` | P1 | **Run Details cannot delete a run or change its privacy.** The screen offers only Export GPX and Analyze run. `docs/runs-design/RUNS_DESIGN_FLOW.md` §5 requires privacy change and Delete (with confirmation, returning to the list), and `RunsRepository` already exposes `delete()` and `update()` — so this is a missing surface over a working API, which the review's own rule ("do not hide a missing feature behind a visually complete screen") calls out. Deletion is also a privacy control: a runner who records a route by mistake has no in-app way to remove it. | Add a privacy toggle and a confirming Delete to Run Details, wired to the existing repository calls, returning to the list on success. Accept with a negative test that another runner cannot delete or unhide the run. |
| `RUNPAR-004` | P2 | **Pace and short-duration units are never translated.** `ZidRunFormat.pace()` hardcodes `"/km"` and `durationShort()` hardcodes `"h"`/`"m"`, while distance uses the translated `runs_unit_km`. In Arabic the same card therefore reads "1.19 كم" beside "3:59/km", and the week card shows "7m". The bidi isolation around these tokens is correct and must be kept. | Thread localized unit strings into `ZidRunFormat` (it is a pure object in `core:design` with no resource access today) and add the keys to all three locale files. Accept when no Latin unit remains in the Arabic Runs surfaces and `check:native-i18n` still passes. |
| `RUNPAR-005` | P2 | **The pre-start "GPS ready" chip is decorative.** `StartRunScreen.kt:153` renders a fixed `ReadyChip` with a fixed green tint and no state input, so it reads "GPS ready" before any fix exists and — reproduced on device — keeps reading "GPS ready" while location permission is denied, directly contradicting the red "Location access is needed…" message below it. The approved flow requires a real ready/acquiring distinction. The denial, retry, and granted states themselves behave correctly. | Drive the chip from the recorder's `gpsStrength` and the permission state (`Unknown` → acquiring, denied → an explicit denied state). The neighbouring "Private" chip is hardcoded the same way and should follow the account's real default. Accept when the chip disagrees with neither the permission state nor the first fix. |
| `RUNPAR-006` | P2 | **The remote feature kill switch is dead code.** `/api/v1/config` still returns `runs:false, coach:false`, and its comment claims "the app hides the tab on this flag", but the only consumer of `features` is `googleSignIn`; `SHELL_TABS` is a static list. Runs and Coach are shipped and reachable regardless. An operator disabling a misbehaving feature remotely would see no effect. | Either honour the flags in the shell or delete them and the comment. If honoured, flip both to `true` in the same change, since the features now exist. Accept by toggling the flag server-side and observing the tab appear/disappear without a new binary. |
| `RUNPAR-007` | P3 | **The finish dialog promises photos that do not exist.** "Finish this run?" says "You can add a title and photos next"; the save screen offers title, notes, and effort only. | Either add photo attachment or drop the word from the string in all three locales. Accept when the copy matches the screen. |
| `RUNPAR-008` | P2 | **Capacitor replaces the brand wordmark with typed text.** `native-header.tsx` `getScreenTitle()` returns the literal string `"ZidRun"` for `/`, so the Capacitor home screen shows a plain `<h1>` where the native app and the approved native design both show the ZidRun wordmark. Capacitor-only; contradicts the review's brand invariant ("do not replace them with typed text"). | Render `ZidRunLogo` in the native header's root state. Accept against `docs/native-design/images/04-races-page.png` in all three themes. |
| `RUNPAR-009` | — | **Test-environment limitation, not a product defect.** The Pixel_8 API 37 emulator's WebView crashes with `SIGILL (ILL_ILLOPN)` in `libwebviewchromium.so` (`WV.yh1.onTrimMemory`); the identical signature also kills stock `com.android.chrome` on this image. It made Capacitor in-app navigation unreliable and blocked the Capacitor Runs/Coach/Account journeys. | Re-run the Capacitor half on a physical device or an emulator image with a working WebView before any Capacitor parity claim. No ZidRun code change implied. |

### Open findings from the 2026-08-01 Coach parity review (`NATIVE-006`)

Found on the same Pixel_8 emulator (Android 17 / API 37, 320×640 @160dpi), native
`dz.racedz.nativeapp.debug` v0.7.0, Capacitor `dz.racedz.app` v2.1, both against `http://10.0.2.2:3003`.
The nine defects fixed in the same session are described in the evidence row below and are not
repeated here.

| ID | Severity | Finding, reproduction, and expected behaviour | Proposed fix and acceptance |
|---|---|---|---|
| `COACHPAR-001` | P2 | **Voice input and TTS are not wired on native.** Capacitor's composer carries a microphone button beside Send and the server has `/api/coach/transcribe` and `/api/coach/tts`; the native composer has neither. This is a missing feature rather than a dead control — nothing on the native screen promises voice — but the approved flow (`COACH_DESIGN_FLOW.md` §4, "Voice note with visible recording/transcribing state") counts it as part of the conversation, and a runner who cannot type comfortably in Arabic loses the surface entirely. | Add an audio-record permission flow, a visible recording/transcribing state that does not move the composer, and an upload to the existing transcribe endpoint; play replies through the existing TTS route with the disk cache the guided cues already use. Accept when recording, transcribing, failure, and denial states are verified in all three locales, and when a device with no microphone permission shows the control as unavailable rather than broken. |
| `COACHPAR-002` | P2 | **Native has no Coach memory or privacy surface.** Capacitor's Coach header carries a **Memory** button opening "What your coach remembers" — provenance per item, confirm/forget, export, delete-all, and the statement that health, injury, and medical details are never stored there. The whole surface is absent from native, so a native-only runner cannot see or delete what the coach remembers about them. `/api/coach/memory` exists (GET incl. `?scope=export`, PATCH confirm, DELETE all) but has no `/api/v1` equivalent. | Add an additive `/api/v1/coach/memory` reusing the same handlers, and a native screen reached from the Coach overview. Accept with a negative test that runner B cannot read, confirm, or delete runner A's memory, and a check that no health-kind row is ever returned. |
| `COACHPAR-003` | P2 | **A native goal cannot be edited, so the coach's language is fixed at setup.** Capacitor offers **Edit goal** (the same five steps, prefilled, ending in "Save changes") and a coach-language selector in the Coach header. Native creates a goal once; there is no edit path, so changing the coaching language, target date, availability, or health context means creating a *new* goal — which `createCoachGoal` supersedes the active plan for. The language control added to the native Review step therefore only ever applies to a brand-new goal. | Add an edit flow reusing `CoachOnboardingScreen` prefilled from the active goal against `updateCoachGoal`, plus a standalone language control that calls `updateCoachGoalSettings`. Accept when changing the language rewrites the next generated week in that language without discarding the active plan. |
| `COACHPAR-004` | P2 | **The training plan is not age-aware; only the coach's prose is.** `AdaptivePlannerInput` (`src/lib/coach/adaptive-planner.ts`) carries goal, experience, volume, availability, and metrics — but no age or date of birth. Verified by the profile matrix: cases C (24, advanced, 7 days) and G (68, advanced, 7 days) both produce seven running days with zero rest, differing only by the weekly volume each runner reported. `MAX_RUN_DAYS.ADVANCED = 7` deliberately permits a seven-day week, and for a 24-year-old that is a defensible choice; applying it unchanged to a 68-year-old is the part that needs a decision. Age *is* sent to the AI context, so the written advice can be age-aware while the schedule is not. **Shared server: both clients are affected identically, so this is not a parity defect.** | An owner/product decision before a code change, because it alters plans for every existing runner: either feed age into the planner and taper `MAX_RUN_DAYS`/intensity for older bands, or record that experience is deliberately the only load input and say so in the Coach copy. Accept via `scripts/test-adaptive-planner.ts` cases that pin whatever rule is chosen, plus the qualified sports-health review that `SEC-002` already requires before health personalisation ships. |
| `COACHPAR-005` | P3 | **The native trial banner does not say how much trial is left.** Native shows a bare "Free trial" pill; `COACH_DESIGN_FLOW.md` ("Subscription and access states") asks for remaining time or usage as a calm inline banner. `/api/v1/coach` already returns `trialEndsAt` and `usage.daily`/`usage.monthly` against the tier limits, so the data is on the client and unused. | Render days remaining and today's message count in the pill or a line beneath it. Accept in all three locales at 320dp without clipping, and on the last day of a trial. |
| `COACHPAR-006` | P3 | **Capacitor-side truncation at 320dp — do not copy.** Capacitor's "Your training so far" card clips its own column headers to "LAST 4 W…" and "AVERAGE …", and the conversation composer's placeholder overflows its box. Capacitor-only; the native equivalents render fully. Recorded so the next parity pass does not mistake the native rendering for a divergence. | Widen or wrap those labels in `coach-overview.tsx`, and give the composer's textarea room for its placeholder. Accept against a 320dp viewport in en/fr/ar. |

## Current evidence

| Date | Evidence | Result |
|---|---|---|
| 2026-08-01 | `NATIVE-006` Coach parity review — Capacitor baseline captured, **9 defects found and fixed**, 8-case profile matrix passed | Ran the Coach comparison at `ef0cece` on `main`, Pixel_8 emulator (`sdk_gphone16k_x86_64`, Android 17 / API 37, 320×640 @160dpi). Packages: Capacitor `dz.racedz.app` v2.1 (Capacitor 6.2.1), native `dz.racedz.nativeapp.debug` v0.7.0. Both against the same local server (`http://10.0.2.2:3003`, `AUTH_URL`/`NEXTAUTH_URL` verified by following `/account/coach` → `307` to the `10.0.2.2` origin). Test accounts: the seeded demo runner `runner@example.com` for the Capacitor baseline, a purpose-made isolated `coach.tester@zidrun.test` for native, and 12 disposable `coachmx-*` runners created and deleted by the new contract suite. **The owner's personal account was not used.** **`RUNPAR-009` no longer blocks Capacitor:** the WebView fault did not recur, and the full Capacitor Coach baseline was captured — Overview (goal chip, Memory, coach-language selector, week totals, streak, "No active plan"), the inner Overview/Plan/Sleep/Coach tabs, Plan ("Draft plan" → Generate weekly plan), Sleep (Hours/Times/Describe, night, note, history), Conversation (suggested questions, mic + send, "Safety boundary", "Stored context"), Memory ("What your coach remembers", provenance, "never stores health, injury, or medical details"), and the five-step Goal Setup (Goal / Background / Availability / Health / Review) end to end. **Plan generation is deterministic** (`buildAdaptivePlanForGoal`, no AI call) and localised from `goal.preferredLocale`, which is what made the matrix assertable rather than eyeballed. **Nine defects found and fixed, each re-verified:** (1) **P1 safety — the phone was shown less caution than the website.** `GET /api/v1/coach/interactions` flattened the reply to its `summary` string, silently dropping `warningSignals`, `requiresProfessionalAdvice`, `progressAssessment`, `recoveryAdvice`, `dataGaps`, and `followUpQuestion`. On a real reply that meant a native runner never saw "don't turn the long run into a speed session today" or the heat warning that the same reply showed on the website. The reply now travels structured (shared mapper in `src/lib/api/v1/coach.ts`) and native renders it in the website's order — professional-advice banner, summary, assessment, positives, warnings — plus recovery, data gaps, and the follow-up question. Icons and position carry the positive/warning distinction, not colour alone. (2) **P1 — the Coach tab errored out for anyone who had ever asked a question.** `GET /api/v1/coach` returned the raw `response` JSON object as `latestReview.text`, where the client's contract says string; deserialisation failed and the whole tab showed "Something went wrong". Invisible until a test account actually had an interaction. Both routes now share one mapper. (3) **P1 — the conversation swallowed the question.** The client assumed generation was asynchronous and polled for a `PENDING` row, but `getConversationHistory` only ever returns COMPLETED/BLOCKED, so no such row could exist: the poll ended on its first pass, the "generating" state never engaged, and the question vanished for 10–40 s with nothing on screen. Generation is in fact synchronous (measured **11.9 s** through the mobile API), so the state is now local and the question stays visible under "Your coach is thinking…". (4) **P1 — a 30 s read timeout against a ~12 s-and-rising generation.** Aborting client-side does not cancel the work: the server still finishes and still counts the message against the runner's daily quota, so a timeout charged a credit for an answer never shown. Coach asks are now marked slow at the HTTP layer (120 s read timeout, marker header stripped before the request leaves the device). (5) **P1 — an Arabic-speaking runner got an English training plan.** The plan text and the coach's replies are localised from `goal.preferredLocale`, and the native client hardcoded `"en"` with no way to change it. Onboarding now defaults it from the app locale and offers an explicit **Coach language** choice on the Review step. (6) **P1 crash — the plan week died on data it did not expect.** `weekDays.first { … }` threw `NoSuchElementException` when no session fell inside the rendered week. Root cause underneath it: `/api/v1/coach/plan` applied `AT TIME ZONE 'Africa/Algiers'` to `scheduledFor`, which is `timestamp *without* time zone` — on a naive column that operator *attaches* a zone instead of converting, shifting the window by Algiers' offset and pulling in the following Monday's 00:00 UTC session. Fixed on both sides: the bounds are now converted (not the column), and the client draws any workout that lands outside the named week rather than crashing. (7) **P1 — "Log this run" did not carry the workout.** `CreateRunRequest` had no `workoutId` field at all, so a run logged from the plan saved fine and simply never counted toward its session. Threaded from the plan through the recorder, the outbox, and the save; pinned by `WorkoutHandoffTest`, including survival of an app kill. (8) **P1 — "Analyze run" was not about the run.** Run Details navigated to the general conversation with no `runId`, so no `POST_RUN` interaction was created and no context chip named the run. It now carries the run and offers an explicit "Ask for a review" — offered, never auto-sent, because the analysis spends one of the runner's daily messages. (9) **P1 — finishing onboarding returned to the setup prompt.** The Coach view model loaded once when the tab was first composed, so a runner who had just created a goal came back to the very "Set up your coach" screen they had finished; the tab now re-reads on resume without blanking. **Also fixed:** the goal card printed the raw enum `TEN_K`; "Sessions completed" broke mid-word into "Sessi/ons/compl/eted" beside the adherence ring at 320dp; the wizard kept one scroll position across steps, dropping the runner two-thirds down step 3; the consent checkbox's sentence-long label was not part of its touch target; a generic English server message ("Something went wrong. Please try again.") rendered untranslated inside Arabic and French screens (now replaced app-wide for the generic codes only, so specific validation messages still reach the user); the day strip opened on Monday with today's session scrolled off the right edge; two sessions on one day silently hid one (`associateBy` → `groupBy`); and a plan starting on Monday reported "No plan yet" moments after setup (`hasPlan` now asks whether a plan exists, separately from what falls in this week). **Built to parity:** native onboarding is now the approved five steps — Goal (all seven goal types incl. `OTHER` with a required written goal, target date, optional distance and time), Background (experience, weekly volume, and the optional years/peak/longest/recent-result/resting-HR/weight/height set), Availability (7-day picker plus a long-run day offered only among the chosen days, and constraints), Health & safety (current injuries, injury history, the server's eight chronic conditions, health notes), and Review (summary, coach language, explicit consent gating submission). Every optional field is sent only when typed — asserted per matrix case that no body or health value was invented. **Move and "I can't today"** are built on a new additive `PATCH /api/v1/coach/workouts/[id]` reusing the website's own `skipWorkout`/`rescheduleWorkout`/`setWorkoutSkipReason`, with the day picker bounded by the plan's real `endsOn` so it cannot offer a date the server would refuse, a supportive reason picker that permits skipping without a reason, and a server-authoritative reload rather than an optimistic tick. **Profile matrix — all 8 cases pass** (`npm run test:coach-mobile`, new): A 18-25/M/beginner/2d → 2 sessions 6.0 km; B 18-25/F/beginner/4d → 4 / 10.0 km; C 18-25/M/advanced/7d → 7 / 38.7 km; D 18-25/F/advanced/2d → 2 / 30.3 km; E 60-70/M/beginner/2d → 2 / 6.0 km; F 60-70/F/beginner/4d → 4 / 10.0 km; G 60-70/M/advanced/7d → 7 / 33.1 km; H 60-70/F/advanced/4d → 4 / 24.7 km. Every session falls on a day the runner selected, none exceeds the planner's documented run-day ceiling, and a first week for a runner with no history is entirely RECOVERY/LONG_RUN — conservative by construction. "Pro" is mapped to the API's real `ADVANCED`; no `PRO` enum was invented and no minors were created. **Boundary cases refused server-side, not merely greyed out:** past target date, one training day, duplicate days, `OTHER` with no custom goal, a 301-character goal, negative and non-numeric weekly distance, and a long-run day outside availability; seven training days is accepted. **AI safety and context parity:** a live reply carried 4 warning signals (including the reported chronic condition from onboarding), 4 recovery items, 3 named data gaps, and a follow-up question; `safety` came back `CAUTION` with `requiresProfessionalAdvice` false; `usedSignals` listed only signals actually supplied; provenance (`promptVersion`, `contextVersion`, `contextHash`) was recorded. A scripted scan of the stored interaction found no email, phone, precise coordinates, or prompt/context text, and the API deliberately does not echo `usedSignals` or `memoryCandidates` to the client. **Authorization negatives pass:** runner B cannot skip or move runner A's workout (404, with `NOT_FOUND` so the refusal does not confirm the workout exists), an unauthenticated caller is refused, a session cannot be skipped twice, a workout cannot be moved into the past, and another runner's transcript contains none of the first runner's conversation. An expired trial answers 200 with `tier: NONE` and no goal, plan, or adherence loaded. **Verified on device:** onboarding end to end (all five steps, with sex/birth date correctly *not* re-asked for a profile that already had them, and the goal reaching the DB with `preferredLongRunDay=5` and `chronicConditions=["ASTHMA"]` and nothing invented); the populated overview; the plan week with move and skip, both confirmed against the database; a live coach question with the full structured reply; and Coach in **light, dark and race** and in **English, French and Arabic RTL** — Arabic fully mirrored, French using a decimal comma, and the plan text correctly staying in the *goal's* language rather than the device's. **Performance:** cold launch to first frame 669 ms (`am start -W`); PSS 125.5 MB at Coach and 122.9 MB after Coach → Plan → back → Coach (no growth); 4.5 % janky frames over 244 frames scrolling the overview with 0 missed vsyncs; no ANR or crash after the fixes. **Gates:** `./gradlew lintDebug testDebugUnitTest` — 0 lint errors across all 8 modules, 35 native unit tests, 0 failures; `check:native-i18n` 459 keys at en/fr/ar parity; `npx tsc --noEmit` and `eslint` clean; new `npm run test:coach-mobile` 124/124; `npm run test:mobile-api` 87/87; `npm run test:coach` unchanged (context evals, adaptive planner 68/68, memory 36/36). **Open, not fixed** (details in `COACHPAR-001`–`COACHPAR-006`): voice input and TTS are absent on native; there is no Coach memory/privacy surface; a goal cannot be edited, so the coach's language is fixed at setup; the deterministic planner takes no age input, so an older advanced runner gets the same seven-day structure as a young one (shared server — an owner decision, not a parity defect); the trial pill does not say how much trial is left; and Capacitor truncates two of its own labels at 320dp. **Not tested:** TalkBack, offline behaviour and retry for the coach mutations, force-stop during a generation, rotation mid-onboarding, logout clearing of cached Coach data, and the Capacitor half of the run-analysis and sleep-save journeys. **Status: PASS WITH OPEN P2/P3** — no P0 or P1 remains open on the Coach surfaces reviewed. |
| 2026-07-31 | `NATIVE-006` parity review — native Runs verified on device; **4 defects found and fixed**; Capacitor half **blocked by an emulator WebView fault** | Ran the Capacitor-vs-native comparison at `ef0cece` on `main`, Pixel_8 emulator (`sdk_gphone16k_x86_64`, Android 17 / API 37, 320×640 @160dpi, gesture nav). Packages: Capacitor `dz.racedz.app` v2.0, native `dz.racedz.nativeapp.debug` v0.7.0-debug (versionCode 7). **The previous adb wedge is gone.** Both apps were re-pointed at the same local server (`http://10.0.2.2:3003`, `npm run dev:lan`) — the installed Capacitor build had been synced to production `https://zidrun.com`, so the first action was a re-sync + reinstall, without which no data comparison would have been valid. Test account: seeded demo runner `runner@example.com` (50 runs, `profileComplete`, coach `SUBSCRIBED`, `TEN_K` goal, no active plan). Location permission was **explicitly granted by the tester** (denial → retry → "While using the app"); no permission was silently pre-granted. **Native run lifecycle passes end to end:** hold-to-start → GPS acquiring (map correctly withheld, no stale city — `RUN-001` behaviour holds) → live metrics → background recording via the location foreground service (`vis=PRIVATE`, distance advanced 0.18→0.43 km while backgrounded) → pause (Discard/Resume/Finish) → confirm dialog → explicit Save/Discard (`RUN-003`) → server-authoritative save (row `1e39cb3c…`, `isPublic=false`, so the private default holds) → detail with splits and compact-axis elevation/pace charts (`RUN-002`). **Four defects found on device and fixed in this session, each re-verified on device after the fix:** (1) **P0 — a saved run was invisible in the app.** `/api/v1/runs` is a delta feed ordered `updatedAt` ascending; `RunsRepository.list()` made a single un-cursored call, so a runner past one 50-run page saw only their *oldest* 50 runs, permanently. The just-recorded run was absent from history, "Latest run" was stale by two weeks, and "This week" read 0.00 km. `PageMeta` did not even carry `nextCursor`, so the cursor was unreadable. Fixed by adding `nextCursor` to `PageMeta` and following the cursor in a bounded loop (`MAX_PAGES = 40`), accumulating by id so cross-page tombstones apply in order; a mid-way page failure now keeps the pages already fetched instead of discarding the history. After the fix the overview immediately shows the new run and "This week 1.19 km · 1 run · 7m" — `RUN-005` now demonstrably holds on native. (2) **P1 — the run map rendered a black band and was geometrically wrong.** Tiles used `Modifier.size(256.dp)`, which is only a *preferred* size, so the parent's height constraint squashed every tile to the map's own height while the offsets stayed on the true 256px grid — leaving an unpainted row and scaling the tile artwork out of register with the route drawn over it (top and bottom halves showed discontinuous parts of Algiers). Fixed with `requiredSize` plus `clipToBounds` on the map, since tiles now intentionally overhang. Affected all three map surfaces (live run, save summary, run detail). (3) **P1 — the "View history" entry point was invisible.** `ZidRunSectionHeader` applies `fillMaxWidth()` internally, so the hand-rolled chip beside it in `RunsOverviewScreen` was left a 40dp strip; its label wrapped into a ~210dp column of clipped glyphs. The control added in `ef0cece` specifically because a tester could not find their run list rendered as nothing at all — and its 210dp height was also the empty gap above the latest-run card. Fixed by weighting the header. (4) Verified the fix holds for the longer French label ("Voir l'historique"). **Gates after the fixes:** `:app:assembleDebug` clean, `lintDebug` 0 errors across all 8 modules, 31 native unit tests pass, `check:native-i18n` 370 keys at en/fr/ar parity. **Themes and locales checked on device:** Light/Dark/Race and EN/FR/AR all render; Arabic is fully mirrored (logo, chip chevron, bottom-nav order, alignment) and correctly does *not* mirror route geometry; French uses a decimal comma. **Capacitor half is blocked:** the emulator's WebView/Chromium build crashes with `SIGILL (ILL_ILLOPN)` inside `libwebviewchromium.so` (`WV.yh1.onTrimMemory`) with no ZidRun frames on the stack — and the identical signature also kills stock `com.android.chrome` on this image, so it is an emulator/WebView-image fault, not a ZidRun defect. It made in-app navigation unreliable, so the Capacitor Runs/Coach/Account journeys were **not** exercised and no Capacitor parity result is claimed for them. Capacitor evidence that *was* obtained: cold launch 3599–3655 ms vs native 640 ms (`am start -W`, twice); PSS 130 MB vs native 148 MB; and the shell renders a typed `<h1>` "ZidRun" (`native-header.tsx` `getScreenTitle`) where native shows the real brand wordmark. **Open, not fixed** (details in the P0 mobile-field table and `NATIVE-006`): `RUNPAR-001` hold-to-begin foot silhouette clips its own "Hold / to begin" label; `RUNPAR-002` history rows drop the "km" unit when duration ≥ 1 h; `RUNPAR-003` Run Details has no Delete or privacy control though the API and flow require both; `RUNPAR-004` `ZidRunFormat` hardcodes "/km" and "h"/"m", so Arabic mixes "كم" with "/km"; `RUNPAR-005` the pre-start "GPS ready" chip is hardcoded and stays green with location denied; `RUNPAR-006` `/api/v1/config` still advertises `runs:false, coach:false` and nothing reads them, so the kill switch is dead; `RUNPAR-007` the finish dialog promises photos the save screen does not offer. Coach and Account were reached but not exercised against the data contract; that remains open. |
| 2026-07-31 | Two-stage onboarding, gated server-side (`NATIVE-004`/`NATIVE-006`) | Split onboarding in two, per the product decision: a **user onboarding** after registration (phone, birth date, gender, wilaya, city — exactly the fields `raceRegistrationSchema` requires) and the existing **coach onboarding** (goal). Both are skippable and both remain editable afterwards in Profile & preferences. **Enforcement is server-side, not a client convention:** `/api/v1/me` now returns a computed `profileComplete`, and `POST /api/v1/races/:id/registrations` refuses an incomplete profile with a new typed `PROFILE_INCOMPLETE` (428) *before* parsing the body. One definition governs both sides — the app uses the flag to decide whether to show onboarding, the endpoint enforces the same fields — so a client-side guess cannot drift when the registration schema changes, and a client that skips the screen entirely still cannot register. The app routes a `PROFILE_INCOMPLETE` straight to onboarding rather than showing five red fields the runner has no way to fill from the registration form. Coach is gated the same way: a subscriber with no goal sees only the setup prompt, because there is genuinely no plan to show and nothing for the coach to reason about. Every sign-in routes through onboarding, which forwards straight to the shell when `profileComplete` is already true — deciding there means one place reads the server's own flag instead of navigation guessing from a cached session. Skipping is deliberate: a hard gate on first launch loses people before they have seen anything, and the server ask at registration time is where it actually matters. **Gates:** 87 mobile API assertions (2 new: the gate refuses, and completing the profile flips the flag), `npx tsc --noEmit` and `eslint` clean, 0 native lint errors, 21 native unit tests, 368 native strings at en/fr/ar parity. The new gate correctly broke the existing registration test, whose user had an empty profile; the test now asserts the refusal before satisfying it. **Still unverified on a device.** |
| 2026-07-31 | Native Coach conversation + sleep (`NATIVE-006`, partial) | Completed the five Coach mockups. **Backend (additive):** `GET/POST /api/v1/coach/interactions` and `GET/POST /api/v1/coach/sleep`, both reusing `createCoachInteraction`, `getConversationHistory`, `createSleepEntry`, and `getSleepEntries` — this facade adds no AI behaviour of its own, which it could not do without putting model output outside the safety and memory-governance rules the plan requires it stay inside. Entitlement travels *with* the transcript so the composer knows before the runner types whether an answer is left today; discovering the limit only after sending would waste the message they wrote. The conversation DTO keeps `safety` as its own field rather than folding it into the reply text, because the website renders safety notices deliberately and flattening one into prose strips a guardrail. **Native:** conversation screen (newest-at-bottom transcript, composer disabled while a reply is generating so a second question cannot spend another credit before the first is answered, distinct rendering for PENDING/BLOCKED/FAILED — a BLOCKED reply says the coach cannot answer rather than "something went wrong", since inviting a rephrase is the opposite of what a block is for) and sleep/recovery (log last night, recent nights as bars; short nights tinted differently without the UI pronouncing them "bad", which is the coach's call). Replies are generated asynchronously, so the client polls the transcript — bounded to ~60s — rather than holding a request open across a phone falling asleep. Sleep is only ever sent when the runner types it: no sensors, no Health Connect, nothing read from the device. **Gates:** `npx tsc --noEmit` and `eslint` clean, 0 native lint errors, 21 native unit tests, `test:coach` 36/36 unchanged, 359 native strings at en/fr/ar parity. A raw `&` in a new string broke resource merging and was caught by the build; all three locale files are now entity-safe. **Not verified on a device** — the emulator has been unreachable since the plan-week work, so the conversation, sleep, plan-week, onboarding, and populated overview are all covered only by the build and the DTO/schema contract. Remaining Coach gaps: the plan's "Log this run" does not yet pass `workoutId` to the recorder, "Move"/"I can't today" are not built, and voice input (`/api/coach/transcribe`) and TTS playback of replies are not wired. |
| 2026-07-31 | Native Coach surfaces (`NATIVE-006`, partial) | Replaced the Coach placeholder with real surfaces, all server-backed. **Backend (additive):** `GET /api/v1/coach` (overview), `GET/POST /api/v1/coach/goals` (onboarding state + goal creation), `GET /api/v1/coach/plan` (this week). All reuse the website's own helpers — `getCoachEntitlementWithUsage`, `getTodayGuidedWorkout`, `getPlanAdherence`, and `createCoachGoal` — rather than reimplementing anything, so validation, the sex/birth-date write-back to `User`, plan generation, and the safety/governance path behave identically on both clients. Deliberately NOT `getCoachDashboard()`: that returns ten runs, two full plans with every workout, ten interactions, sleep history and tips — megabytes to render six cards. The plan endpoint is bounded to one Mon–Sun window computed in `Africa/Algiers` (not UTC, not the device clock) so a runner opening the app late Sunday sees the week they just finished. A `NONE` entitlement returns **200 with empty sections, not 402**: the app must show a subscribe prompt, and an error status is indistinguishable from a failure to the client's retry logic; nothing beyond the tier is loaded for an unsubscribed runner, so the shape of a paid feature is not handed to someone who has not paid. **Native:** new `:feature:coach` module — Coach overview (goal, today's workout with target effort and instructions, this-week adherence ring, next workout, latest review), coach onboarding, and the plan week (day strip built from the server's workouts so a rest day is genuinely a rest day in the plan and not a gap the client invented; "Log this run" offered only for `PLANNED` sessions, since offering it on a completed one invites a duplicate). Onboarding asks only what `createCoachGoalSchema` requires plus sex/birth date when the account lacks them; the website's optional fields (resting heart rate, peak volume, injury history) are omitted rather than sent as invented defaults, because a fabricated resting heart rate would feed the plan as though the runner had measured it. Subscribing opens the website's existing payment-proof flow in a custom tab rather than shipping a second version of it. **Gates:** `npx tsc --noEmit` and `eslint` clean, 0 native lint errors, 21 native unit tests, `test:coach` 36/36 unchanged, 342 native strings at en/fr/ar parity. **Not verified on a device:** the emulator wedged (process alive, adb unreachable) before the populated dashboard, onboarding form, or plan week could be exercised, so all three are covered only by the build and the DTO/schema contract. Coach conversation (04) and sleep/recovery (05) are not started. |
| 2026-07-31 | Native Android phases 2–5 (`NATIVE-003`/`NATIVE-004`, partial `NATIVE-006`), commit pending | Built the additive `/api/v1` mobile facade and the native auth/races/account/registration slice on top of phase 1. **Backend (additive only — no website or Capacitor behavior changed):** one migration (`20260731000000_mobile_api_v1_sessions`) adding `MobileSession`, `MobileAuthCode`, and `MobileIdempotencyRecord`; a shared envelope/typed-error/pagination layer (`src/lib/api/v1/`) where every endpoint answers `{data,meta}` or `{error:{code,message,details}}` with an `X-Request-Id` and `Cache-Control: private, no-store`; explicit response DTOs so no Prisma row is ever returned (password hashes, MFA secrets, security stamps, national ID, payment-proof paths, and organizer bank details for races the caller has not entered are all excluded by construction). Endpoints: `auth/{login,register,refresh,logout,logout-all,resend-verification,authorize,token}`, `me{,/preferences,/registrations,/sessions,/export,/deletion-request}`, `races{,/:id}`, `races/:id/registrations`, `registrations/:id/payment-proof`, and `config`. Auth reuses the *same* `verifyLoginCredentials`/`verifyMfaCode`/`createRaceRegistrationForUser` helpers the website uses, so password checking, the email-verified gate, MFA, capacity locking, and duplicate protection cannot drift between clients. Sessions are 15-minute HS256 access tokens (carrying `securityStampAt`, re-checked against the live `User` row on every call) plus 60-day rotating refresh tokens stored only as SHA-256 hashes; replaying a rotated token is treated as theft and revokes the whole device family. Google sign-in is a real OAuth 2.0 authorization-code + PKCE (S256) flow through the *system browser* — `/api/v1/auth/authorize` hands off to the existing `/login` page (so Google, blocked accounts, and the MFA second-factor page all apply unchanged) and returns a single-use code over `zidrun://auth/callback` that is worthless without the verifier the app never transmits. Registration accepts an `Idempotency-Key` so a retry after a dropped response replays the stored 201 instead of registering twice. **Native app:** grew to 8 Gradle modules (`:app`, `:core:design`, `:core:network`, `:core:auth`, `:feature:{auth,races,account,registration}`) with a version catalog and a manual `AppContainer` service locator (no annotation processing). Tokens live in `EncryptedSharedPreferences` (Keystore AES-256-GCM) with `allowBackup=false` plus explicit backup/transfer exclusions; refresh is single-flight behind a mutex specifically so a burst of 401s cannot self-inflict the server's reuse-detection revocation. Release builds are HTTPS-only (`network_security_config` permits cleartext solely for the emulator's `10.0.2.2` host loopback in debug), and there is deliberately no in-app server-URL field. Screens shipped: login (with MFA step and email-verification handoff), create account, races list (debounced search, infinite scroll, loading/empty/offline/error/retry), race detail (server-authoritative CTA), account hub, profile & preferences, privacy & data (devices, sign-out-everywhere, deletion request), my registrations, and the three-step registration with private payment-proof upload via the Android photo picker (no storage permission). **Verified on a Pixel_8 API 37 emulator against `npm run dev` at `10.0.2.2:3003`, end to end:** sign in → races → race detail → register → pay → upload proof → account → preferences, plus deep links (`zidrun://race/<slug>`), offline, and revocation. Server state confirmed in Postgres after each step (registration `PENDING`/`MANUAL_REVIEW`, proof stored under the non-public `/uploads/payment/*` path, `availablePlaces` decremented, preferences persisted). Bumping `securityStampAt` server-side signed the device out on its next call with the correct localized reason. **Eight real bugs were found by this testing and fixed:** (1) kotlinx-serialization omits properties equal to their default, so `acceptedTerms=true` never reached the wire and every registration was rejected — fixed with `encodeDefaults = true` and pinned by a test; (2) `LaunchedEffect` keyed on a `Uri` did not re-fire for an identical deep link, so opening the same race link twice did nothing — fixed with a monotonic event id; (3) a deep link arriving during the splash was wiped by the splash's own `popUpTo(inclusive)` — now parked and replayed after handover; (4) the saved theme did not survive a cold start or the Activity recreation a language change triggers — now mirrored to plain prefs for the first frame and reconciled from `/api/v1/me` at launch; (5) relative image paths from the API were unloadable by Coil (no page origin), leaving blank cards — resolved against the API base; (6) a failed image still reserved its 220dp banner, leaving a void above the title — now collapses; (7) the offline message was a hardcoded English literal from the network layer, showing untranslated in French and Arabic — client-synthesized offline failures now use the localized string; (8) "Continue with Google" was shown even where the server has no Google client configured — now gated on `/api/v1/config`, defaulting to hidden. Also fixed: status-bar/keyboard insets on full-screen routes, distance pills squashing their text, and the search field's floating label colliding with the screen title. **Gates:** `./gradlew lintDebug testDebugUnitTest` across all 8 modules — 0 lint errors, 76 advisory warnings (dependency-version/vector-path/monochrome-icon), 13 unit tests pass (API envelope/error mapping, single 401 refresh-and-replay, reuse-detection reason preservation, request encoding, PKCE RFC 7636 conformance); new `npm run test:mobile-api` contract suite — 44/44 pass, including anti-enumeration (unknown email is byte-identical to a wrong password), refresh rotation + reuse detection + family revocation, security-stamp and blocked-account revocation, idempotent replay vs. genuine duplicate 409, and object-level authorization (another user cannot read a registration's payment proof or see it in their list); `npx tsc --noEmit` and `eslint` clean; `test:mfa` and `test:registration` pass unchanged. **Not done:** Runs and Coach (phase 6) are still tab placeholders and gated off in `/api/v1/config`; no Room/WorkManager/offline outbox, no FCM, no signed release build, no physical-device or TalkBack pass, and no `https://zidrun.com` App Links autoVerify (the Capacitor package still owns those URLs). `NATIVE-003`/`NATIVE-004` are implemented but not closed — they need the security review, physical-device evidence, and OpenAPI documentation. Not yet reviewed or committed. |
| 2026-07-31 | Native Android phase 1 (`NATIVE-002`), commit pending | Created the isolated `native-android/` Gradle project (Kotlin, Jetpack Compose, Material 3, Navigation Compose) with `:app` (`dz.racedz.nativeapp`, separate from the Capacitor `dz.racedz.app`) and `:core:design` modules; no `.env`, keystore, `google-services.json`, or Capacitor secret was copied. Shipped: splash → app shell with bottom-nav (Races/Runs/Coach/Account) placeholders, light/dark/race theme tokens ported 1:1 from `globals.css`, canonical ZidRun mark/wordmark vector-drawable ports of the real brand SVGs (no redrawn logo), EN/FR/AR string resources (AR carries Algerian Darija phrasing, matching the web's locale boundary) with an Android 13+ `locales_config.xml`, and a phase-1 in-shell theme/language toggle so all three themes and all three locales are reachable without waiting for the real Account settings screen. Verified on a Pixel_8 API 37 emulator with screenshots against `docs/native-design/images/`: light/dark/race all render with correct tokens, and French and Arabic (full RTL mirroring — logo, icon order, bottom nav, and text alignment all flip correctly) both render with the right localized strings. A real bug was found and fixed during this verification: `AppCompatDelegate.setApplicationLocales()` silently no-oped from this Compose-only `ComponentActivity` (no `AppCompatActivity` in the tree), so the in-app language toggle appeared to do nothing even though the OS-level per-app-language mechanism worked fine (confirmed via `cmd locale`); switched to calling the platform `LocaleManager` service directly on API 33+, confirmed fixed by the same screenshot test. `:app:assembleDebug`, `:app:lintDebug`, and `:core:design:lintDebug` all pass (0 lint errors; 20/21 warnings are dependency-version/vector-path/monochrome-icon suggestions, no correctness or accessibility findings). Login, the versioned `/api/v1` client, and the remaining phases (races/registration/account/runs/coach) are not started; this is phase 1 only (project structure, splash, shell/nav, branding, theming, i18n, RTL, accessibility baseline) — not yet reviewed or committed. |
| 2026-07-31 | Runs redesign implementation (`RUN-006`) | Completed the mobile-first Runs overview with weekly momentum, personal records, achievement progress, latest-run card, and quick actions; press-and-hold start interaction; live distance/time hierarchy and recording status; searchable/filterable history with GPS/manual/month filters and immediate saved feedback; and a full expanded post-run details surface with route, splits, charts, performance reading, recovery recommendation, and highlights. Theme-token styling covers light/dark/race modes; copy is present in EN/FR/Algerian Arabic with RTL-aware layouts. Typecheck, lint, i18n parity, production build, focused run tests, and the Coach E2E suite pass. `RUN-006` remains open only for signed-device acceptance across the full flow. |
| 2026-07-30 | Mobile run field findings (`RUN-001`–`RUN-005`) from local artifacts under `docs/runs-issues/` | GPX evidence confirms a cold-start path in the user's current city after the outdoor warm-up; the initial Algiers map was stale/cached-position presentation. Code work now hides the live map until a stable route exists, adds compact chart axes, makes the finished run's local pending state explicit, keeps focused native-app inputs visible above the keyboard, and inserts/highlights newly saved runs without requiring reload. `test:run-incident`, `test:run-stats`, lint, i18n parity, typecheck, and production build pass. All five remain open until signed physical-device retest and release-candidate evidence are recorded; raw GPX/screenshots must remain local and untracked. |
| 2026-07-30 | Native Android option preservation plan | Capacitor remains the current Android release path. A separate native Android evaluation was defined with isolated project/package rules, additive versioned backend APIs, native auth/device sessions, offline GPS/sync/media requirements, parity tests, and a later owner decision gate (`NATIVE-001`–`NATIVE-008`). |
| 2026-07-30 | `SEC-002`/`SEC-004`/`SEC-005`/`SEC-007`/`SEC-010`/`SEC-011`/`SEC-012`/`SEC-013`/`SEC-015` hardening pass, commits `b61a65f`, `756537e`, `ca65a53`, `68050a3`, `62ab04c`, `11b2424`, `6977cb3`, `8ae7dfd`, plus `ae4951f` for `PR-056`/`PR-057` — all local only, not pushed | **`SEC-004`:** full authorization/IDOR audit across coach, organizer, admin, groups, notifications, and races domains found ownership consistently enforced server-side; the one real gap — `toggleKudos()` (`src/lib/social.ts`) checked `run.isPublic` but not the owner's `profilePrivate`, letting a stale runId be kudoed forever after the owner went private — is fixed, with `scripts/test-social-authz.ts` (`test:social-authz`) as negative-path evidence (6 checks). **`SEC-005`:** CSP `script-src` moved from static `unsafe-inline` to a per-request nonce + `strict-dynamic` (`src/middleware.ts` generates it, `src/app/layout.tsx` reads it via `headers()`); added `applyApiOriginGuard()` — `/api/**` state-changing requests that carry the session cookie now need a matching `Sec-Fetch-Site`/`Origin`, or get a 403. Verified live (curl): nonce differs per request, all 65 script/link tags on a rendered page share it, cross-site+cookie POST is blocked, same-origin+cookie POST passes through. A real regression this introduced — React's known nonce hydration-warning false positive rendering Next's dev-only "1 issue" badge, which failed all 21 visual-regression snapshots — was caught by rerunning the suite, root-caused, and fixed with `suppressHydrationWarning` (commit `ca65a53`); confirmed not a deeper problem by reproducing the exact same failure signature at the pre-session commit `7a4e657` in an isolated worktree with none of this session's changes applied — see the `SEC-012` `test:all` note below for that separate, pre-existing issue. **`SEC-007`/`SEC-010`:** race-registration payment proofs (`paymentProofUrl`, scope `"payment"`) were served by Caddy's public `/uploads/*` handler, unlike coach-payment proofs — fixed by 403'ing `/uploads/payment/*` in Caddy and adding `/api/registrations/[id]/proof` (owner/admin/race's-organizer-only, mirrors the existing coach-payment-proof route); verified with seeded data (owner/organizer-member allowed, unrelated user denied, admin allowed, malformed/traversal paths rejected). Rest of the `SEC-007` review (SSRF, CSV escaping, raw-SQL parameterization, redirect handling) found already safe, no changes needed. **`SEC-002`:** added [docs/DATA_INVENTORY.md](docs/DATA_INVENTORY.md), a field-level classification of every data class (purpose/access/retention/export/processor); surfaced open gaps (no self-service export/delete, a fully dead `medicalCertificateUrl` field, no prune job for expired reset/verification tokens) rather than silently closing them. **`SEC-011`/`SEC-012`/`SEC-015`:** ran dockerized `semgrep --config p/owasp-top-ten` and `trivy fs` (Phase 1) — fixed the Dockerfile running as root (now `USER node`, verified with a real image build) and pinned three GitHub Actions from mutable `@v4` tags to commit SHAs; the only other findings were local-`.env` secrets (confirmed gitignored, never committed) and two accepted false positives (Android's mandatory-exported launcher activity; blog JSON-LD `dangerouslySetInnerHTML` built from developer-authored MDX, not user input). `npm audit` clean (0 vulnerabilities), SBOM regenerated (727 components, same as prior run), all migrations reviewed (no destructive `DROP`s). **Found, not fixed:** `main` has no GitHub branch-protection rule at all (`gh api .../branches/main/protection` → 404) — an explicit `SEC-012` acceptance item, left for the owner since it changes push/merge workflow. **`SEC-013`:** added `src/lib/security-log.ts` (`logSecurityEvent`, redacts password/token/proof/GPS/health-adjacent fields, masks emails) and `src/lib/sentry-scrub.ts` (`beforeSend`/`beforeSendTransaction`, wired into all three real `Sentry.init()` call sites — server/edge/client configs had zero scrubbing before this) — strips cookies/auth headers and redacts request bodies. Wired into login success/failure, MFA enroll/disable, password reset requested/completed, admin role-change/block/unblock/delete, every rate-limit trip (one call site covers ~20+ already-limited routes), the new CSRF guard's blocks, and both payment-proof routes' denials. **`SEC-012` `test:all` caveat:** the full `npm run test:all` gate is green except a **pre-existing, unrelated** issue: running the complete e2e suite together (not any single spec in isolation) fails 12 `visual.e2e.spec.ts` snapshots (home/races pages, every theme) because an earlier-running spec file changes race/registration data that the visual baselines don't account for — confirmed by reproducing the identical failure (down to the pixel counts) on the pre-session commit `7a4e657` in an isolated worktree. Not fixed here (a test-architecture change, out of scope for a security pass); `npm run test:e2e:visual` run alone passes 26/26. **`PR-056`/`PR-057`:** wired up `updateGroup()` (previously dead code — no caller existed) behind an admin-only edit form, added `rotateGroupJoinToken()`, and made `removeGroupMember()` auto-rotate the join token so a kicked member's own copy of the link stops working immediately (previously: leaked/shared links worked forever, and removal had no effect on re-joining). `scripts/test-groups-moderation.ts` (`test:groups-moderation`, wired into `test:all`): 9 checks, including seeding a real join, kicking that member, and proving the exact link they used is dead immediately after. `PR-056`/`PR-057` remain open — nothing was pushed or tagged, and remote CI / mobile acceptance haven't run against these commits. Typecheck, lint, and a full production build pass after every commit above. |
| 2026-07-30 | `SEC-001`/`SEC-003`/`SEC-006` partial code hardening | `next.config.ts` no longer sends `X-Powered-By`. Added session revocation: `User.securityStampAt` (migration `20260730000000_add_security_stamp`) is bumped on password reset, MFA enroll/disable, admin block, and role→ORGANIZER; the Node-side `jwt()` callback (`src/auth.ts`) compares it on every request and `session()` (`src/auth.config.ts`) drops `user` when stale, which the existing `!session?.user` guards (middleware included) then treat as logged out. Verified live with Playwright: a real password-reset via the UI revoked a separate already-open session on its very next request, while unrelated normal sessions stayed authenticated across repeated requests. Added `enforceRateLimit` to ~20 previously-unlimited routes (admin approve/reject/reports, coach goals/nutrition/plans/workouts/runs CRUD + GPX export, notification actions, organizer registrations + CSV export, native Google auth, social feed, `/api/me/registrations`). Typecheck/lint/i18n/build all pass. **Accepted exception, recorded by owner decision:** registration keeps telling the caller "this email already has an account" (`src/app/register/actions.ts`) — a deliberate enumeration/UX tradeoff, not closed for `SEC-003`. This is a code-only slice of `SEC-001`/`SEC-003`/`SEC-006` — none of `SEC-001`–`SEC-015` are closed; DB network isolation, backup/restore drills, Cloudflare/host/container config, MFA production proof, the attack-test plan, and the external pentest remain open and need production/owner/external action. |
| 2026-07-30 | Production passive scan (`SEC-001`/`SEC-005`/`SEC-006`) — full run log in [docs/SECURITY_ATTACK_TEST_PLAN.md](docs/SECURITY_ATTACK_TEST_PLAN.md), "Run log" section | Ran the plan's Phase 2/3 production-sanctioned passive checks (dockerized `testssl.sh` + ZAP `zap-baseline.py`, no active scan/brute force/load) against `https://zidrun.com`. testssl.sh: **A+ (96/100)**, no vulnerable protocol/cipher, valid cert, HSTS with preload. ZAP: 52 PASS, 0 FAIL, 15 WARN, all triaged — one already-known issue (`X-Powered-By`, fix committed, awaiting deploy), a few accepted tradeoffs/scanner false positives, and two new real findings fixed in commit `ba0e5cd` (`racedz-locale` cookie missing `Secure`; Caddy `/uploads/*` missing HSTS). Phase 1 and the active-scan/staging phases (4–6) still need `gitleaks`/`semgrep`/`trivy`/`k6` installed and the isolated staging stack running (`stg.zidrun.com` doesn't resolve yet). |
| 2026-07-30 | Mobile static scan + fixes (`SEC-001`/`SEC-007`/`SEC-010`/`SEC-011`, Phase 7), commit `972ee54` | Ran `apkanalyzer` (manifest) and dockerized MobSF static analysis (`opensecurity/mobile-security-framework-mobsf`) against the current **debug** test build (`zidrun-prod-debug-v2.1.apk`) — findings must be re-verified against the actual signed release candidate. **Fixed:** (1) `com.equimaps.capacitor_background_geolocation.BackgroundGeolocationService` shipped `exported="true"` with no permission guard in the plugin's own manifest (any other app could start/bind it) — overridden to `exported="false"` via a manifest-merger `tools:replace` in `android/app/src/main/AndroidManifest.xml`. (2) `@capgo/capacitor-social-login` hard-depends on the Facebook Login SDK even though the app's code only ever calls it with `provider: "google"` (confirmed via grep — Facebook sign-in is never used); its exported `com.facebook.CustomTabActivity` (a BROWSABLE OAuth-redirect catcher) was removed via `tools:node="remove"`. Both verified with a real `./gradlew :app:assembleDebug` — build succeeds, and the merged manifest / rebuilt APK confirm the overrides took effect; only `MainActivity` (required) and three Google-signature-permission-protected components remain exported. **Verified clean:** no cleartext-traffic misconfig; the `google_api_key`/`google_crash_reporting_api_key` "secrets" MobSF flagged are the standard client-embeddable Firebase Android key (confirm in Google Cloud Console it's restricted by package name + SHA-1, nothing to fix in code); everything else in its "hardcoded secrets" list was password/passkey autofill string localizations (false positives). Separately hand-tested the upload pipeline (`/api/uploads`, local only) with 5 crafted files: a polyglot (valid PNG + appended `<script>` payload) and a real JPEG carrying GPS + camera EXIF were both accepted, then **fetched back and confirmed fully sanitized** — the script payload did not survive re-encoding and the EXIF/GPS was completely stripped; a fake image (HTML content, `.png` name+MIME) and an oversized (6 MB) file were both correctly rejected. This is now tested evidence for `SEC-007`'s upload-safety requirement, not just code inspection. |
| 2026-07-30 | Supply-chain scan + remaining `SEC-006` rate-limit gaps, commit `297a665` | Dockerized `gitleaks detect` over the full git history (180 commits, 5.34 MB): **1 hit, reviewed and dismissed** — the RFC 6238 TOTP test vector constant in `scripts/test-mfa.ts` (a public spec value, not a credential). Generated a CycloneDX 1.6 SBOM via `@cyclonedx/cyclonedx-npm` (727 components; kept out of the repo per the plan's evidence-handling rule, regenerate on demand). Closed the last previously-unlimited API routes: races list/detail/categories (public, IP-keyed), `me/appearance`'s anonymous path (IP-keyed), and the coach-payment-proof file route (session-keyed). Typecheck/lint/build pass. **Found but deliberately NOT changed — needs an owner decision first:** `requireAdmin()` (`src/lib/admin.ts`) has no MFA check, and neither current admin account (`admin@zidrun.com`, `admin@racedz.dz`) has MFA enabled yet. Wiring in the hard enforcement the plan's own locked decision calls for ("phishing-resistant MFA is mandatory for owner/admin/superadmin accounts") would immediately lock both accounts out of `/admin` on deploy. Enable MFA on both admin accounts first (`/account/security`), then this can be closed safely. |
| 2026-07-30 | Security surface review and hardening overlay | Existing controls include CSP/HSTS/security headers, Auth.js JWT sessions, TOTP code paths, image magic-byte validation/re-encoding, private coach-payment routing, audit logs, and process-local rate limits. They are not yet security sign-off evidence. `SEC-001`–`SEC-015` are now defined; **0 of 15** security gates are closed pending controlled attack tests, production configuration proof, recovery evidence, and independent review. |
| 2026-07-29 | Owner release decisions and Groups commit `4f0453a` | Groups is included; signed physical-device acceptance is scheduled for 2026-07-30; native app-link/push/Crashlytics verification remains open with engineering assistance. `PR-052` remains open until Elmohassib's production-access decision and ZidRun's resulting Production-page state are verified; Google documentation does not clearly resolve second-app behavior. |
| 2026-07-29 | AI race-post import release hardening | Import stays draft-only, exposes confidence/warnings/all source images, supports category correction, deduplicates source links, and requires an audited human confirmation across every publish path. Migration reset, focused normalization test, publish-guard browser test, lint, typecheck, and production build pass; remote candidate CI remains open. |
| 2026-07-29 | GPX picker/startup-drift hardening, commit `a83e159` | Incident/run-stat tests, lint, typecheck, focused GPX browser test, and production build passed. Raw routes stayed untracked. Push/remote CI and signed-device validation remain open. |
| 2026-07-28 | Pixel 8 debug-emulator incident matrix | Rapid guided skip, force-kill restore, and account switching passed. Sustained-speed auto-pause was inconclusive because Android throttled mock locations. |
| 2026-07-27 | Runs lifecycle/validity hardening | Full local test suite and build passed; suspect activities are excluded; snapshot ownership and guided bounds are covered. |
| 2026-07-26 | Android 2.0 release artifact | Clean production Capacitor sync and signed release build passed. Play App Signing fingerprint and runtime Firebase/Crashlytics verification remain open. |

## Maintenance rules

1. Update the progress line, affected gate, current evidence, and `Last updated` date in this file in
   the same change that affects release status.
2. Never mark owner/external/device work complete from code inspection alone.
3. Remove completed implementation detail; keep one concise evidence row instead of a growing diary.
4. Product reference belongs in `PRODUCT.md`; architecture in `CODEX_CONTEXT.md`; procedures in
   `docs/TESTING.md`, `docs/OPERATIONS.md`, or `docs/MOBILE_ANDROID.md`; incident facts in the incident
   report. None of those files may maintain a separate backlog or percentage.
5. If code and this plan conflict, verify the code, correct this file immediately, and do not create a
   second tracker.
