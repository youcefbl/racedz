<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG ONLY. Overrides the main config to allow plaintext to the developer machine:
  10.0.2.2 is the Android emulator's alias for the host loopback, which is how a debug build
  reaches `npm run dev`. This file is not part of any other variant, so no build that can talk to
  production is able to fall back to cleartext.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
