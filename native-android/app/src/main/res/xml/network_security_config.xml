<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is refused everywhere except the emulator's host loopback (10.0.2.2), which is how
  a debug build reaches `npm run dev` on the developer's machine. Production traffic is HTTPS only;
  there is no way to point a release build at a plaintext host.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
