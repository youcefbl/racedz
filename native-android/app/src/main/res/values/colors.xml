<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- System splash screen background only (androidx.core.splashscreen paints this before Compose
         is up). Matches the background token in the dark theme of globals.css / the existing
         Capacitor splash so first paint reads as the same app regardless of which shell loads. -->
    <color name="splash_background">#080D18</color>
</resources>
